model_name: mistralai/Mistral-7B-v0.1
device: cuda
dtype: float16
revision: null
cache_dir: null
trust_remote_code: false
tokenizer_padding_side: left
auth_token: null
local_files_only: true
device_map: auto
offload_folder: .offload
max_memory: null  # Set to null to use all available memory
use_4bit: false  # Disable 4-bit quantization for better precision
use_flash_attention: true  # Enable Flash Attention 2 for faster inference if available
